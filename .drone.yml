workspace:
  base: /drone
  path: /tmp

pipeline:
  build:
    image: smebberson/alpine-nginx
    commands:
      - env
      - echo build-${DRONE_BUILD_NUMBER}
      - echo commit-${DRONE_COMMIT_SHA:0:8}
      - echo version-${DRONE_TAG}
    when:
      event: [push]